# Learner L4. Build context is cloud-devops/week-07 (not this directory).
# TODO base Python 3.12 slim image; WORKDIR /app; unbuffered stdout.
# TODO COPY requirements.lock; install it; COPY projects/local-stack/ into /app/.
# TODO exec-form command for uvicorn app:create_app --factory, port 8000, host 0.0.0.0.
# Keep service connections and secrets out of build steps.
