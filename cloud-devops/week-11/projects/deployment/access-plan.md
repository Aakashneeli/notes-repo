# Access plan
Status: learner decisions pending

Account alias and region (no credentials):
Human federated identity/MFA:
EC2 role trust principal and permissions:
GitHub role trust subject/audience (if used):
SSH source CIDR and host fingerprint verification:
Inbound rules (port/source/purpose):
Outbound destinations and why:
Subnet route and Internet Gateway explanation:
Host mapping and container listener:
IMDSv2 required; Docker daemon role versus container credential access:
Denied-action test and evidence (local model is not AWS IAM proof):
