# LEARNER: Lesson 4. Build context is cloud-devops/week-11.
# Use Python 3.12 slim; /app workdir; install requirements.lock.
# Copy projects/deployment/fixture/app.py into /app/app.py.
# Run as a non-root user. Use unbuffered logging.
# Exec-form uvicorn app:app --host 0.0.0.0 --port 8000 --no-access-log.
# Do not COPY secrets, .env, .git, logs, or learner evidence.
