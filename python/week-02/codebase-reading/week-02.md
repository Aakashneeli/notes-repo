# Week 2 docs and code reading

Scope: read one toolbox wrapper and its test file. For this foundation week, the supplied small package is an approachable codebase; external OSS reading can come after you can trace it.

- Primary docs URL and exact section:
- Question I went to the docs to answer:
- Tiny experiment and observed result:
- Entrypoint (look in pyproject.toml):
- Trace: CLI arguments → main → core function → stdout/stderr → exit code:
- Module responsibilities:
- Test I read and the behavior it proves:
- Configuration/environment assumptions:
- One pattern I can reuse:
- One question I still have:

Do not copy a docs summary. Write the experiment and your explanation.
