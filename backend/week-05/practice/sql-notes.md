# Learner SQL evidence
Status: not attempted. Learner appends one entry per Q01–Q20; preserve past attempts.
For each: question, predicted rows, your SQL (or queries.sql line), actual rows,
why it works, one changed input, help used. Count independently written queries: unassessed.
