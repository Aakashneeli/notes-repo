# My terminal reference

Write these descriptions yourself after using the commands. Add an actual example you ran and what it returned. Consult the supplied HTML reference only after attempting recall.

| Command | Meaning in my words | My example | Result / common mistake |
|---|---|---|---|
| pwd | shows the full path the current dir is on| | |
| ls |shows all the files and folders in the current dir | | |
| cd |used to navigate thru dirs , and on linux if you type just cd and nothing else , itll take you back to the home directory | | |
| cat |shows the contents of a txt , md file and also used to append and merge files in to a new file | | |
| less |shows long files content in vim | | |
| head |shows first 10 or n numbered lines of the text file | | |
| tail |shows last 10 or n numbered lines of the file | | |
| rg |searching for text in files or dirs line wise. grep but written in rust | | |
| find |finding specific files and folders via terminal| | |
| export |Pass a setting to child processes	 | | |
| chmod | used to modify access persmissions of specific files across users in the machine , a total score of max 7 (4(reading) + 2(writing)  + 1(executing) is put in the command to assingn permissions: chmod 755 file.txt)| | |

Absolute versus relative path:
How a pipe connects commands:
What I check before writing with >:
