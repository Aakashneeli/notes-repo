# Week 1 · Foundation

Open [index.html](index.html) in a browser. Start with lesson 01, then follow Next.

```bash
cd /home/an10/code/notes-repo/os-python-tooling/week-01
xdg-open index.html
```

This is the complete Week 1 study pack for the supplied 12-week AI backend roadmap. DSA is excluded throughout. Weeks 2–12 have a continuity overview, not finished lessons.

## Structure

- `lessons/`: 15 core HTML lessons with worked traces, common mistakes and guided practice, plus a foundation workshop and a CLI/test walkthrough.
- `reference/`: printable quick reference, glossary and troubleshooting.
- `assets/`: shared stylesheet and quiz widget; no external frontend dependencies.
- `plan/`: sequence, scope mapping, assessment and progress checklist.
- `projects/week-01-toolbox/`: learner project with three unfinished core functions, command wrappers, fixture data and tests.
- `practice/`: 12 checked Python exercises, a complete worked CLI, and isolated Git staging/conflict/remote labs.
- `weekly-logs/`, `python/`, `codebase-reading/`: your explanations, mistakes and reading evidence.
- `templates/`: reusable logs and review prompts.
- `roadmap/`: the 12-week plan with DSA removed.
- `MISSION.md`, `NOTES.md`, `AI-USAGE.md`, `RESOURCES.md`: long-term teaching context.

The surrounding notes repo already holds the roadmap's broader topic directories. This course lives within it without duplicating those empty categories. Create learning records only when you demonstrate understanding, following the teach skill format.

## Start today

Read lessons 01–03 and do each command yourself. Write your predictions in `weekly-logs/week-01.md`. Then tell your teacher the output of `pwd`, why the relative path changed, and which command finds error lines.

The project tests intentionally fail until you implement the core functions. See the project README for setup. You make the project Git history as part of learning; this pack does not claim you have finished any exercises.

## The deeper study route

Each core lesson now has a worked explanation before its main exercise. Use two passes: first trace the example, then close it and attempt the practice. Definitions and basic explanations are included locally; official docs provide verification and optional further reading.

After lessons 08–11, do the relevant round of [the foundation workshop](lessons/0016-foundation-workshop.html). It supplies 12 smaller functions, 30 checks, two hints per exercise and explained answers. There are also six broken snippets to diagnose. Before the projects, read [the complete CLI walkthrough](lessons/0017-reading-cli-and-tests.html).

```bash
python practice/foundation-drills/check.py strings
python practice/foundation-drills/check.py collections
python practice/foundation-drills/check.py flow
python practice/foundation-drills/check.py io
```

The learner drill file starts unfinished. TODO output is expected. Run from this course directory. Answer disclosures belong to the smaller drills; the three main project functions remain yours to implement.

## Workspace location

This Week 1 workspace was relocated to `os-python-tooling/week-01/` on 2026-09-10. Planning files now live in `plan/`, matching `python/week-02/`. Learner code, logs and progress were preserved. Historical notes can still mention the old location. Start commands from this weekly directory.

See [relocation verification](VALIDATION.md) and the [read-only workspace checker](maintenance/verify_workspace.py).



i want you so bad i go back on the things i believe , there i just said it scared you'll forget about me....... so young and full of lovi
